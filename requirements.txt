# OLED Dashboard dependencies
#
# ── Always-required (pure Python, safe to pip-install everywhere) ──────────────
flask>=2.0
Pillow>=9.0
psutil>=5.9
requests>=2.28

# ── Hardware-optional packages ─────────────────────────────────────────────────
# These are installed by install.sh via apt first (pre-compiled binaries) and
# then via pip only as a fallback.  Do NOT install this file directly on a Pi
# without ensuring the system build tools + kernel headers are present, or use:
#   pip install --no-deps -r requirements.txt
# and rely on apt for the packages below.
#
# Primary OLED stack (adafruit-blinka wraps Pi I2C/GPIO):
#   adafruit-blinka>=8.0
#   adafruit-circuitpython-ssd1306>=2.12
#
# Fallback OLED stack (luma.oled, used when Blinka is unavailable e.g. DietPi):
#   luma.oled>=3.8
#   luma.core>=2.3
#   smbus2>=0.4
#
# GPIO / LED (installed via apt as python3-rpi.gpio / python3-rpi-ws281x):
#   RPi.GPIO>=0.7
#   rpi_ws281x
